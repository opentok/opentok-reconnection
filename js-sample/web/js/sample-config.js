// Make a copy of this file named config.js. Then replace the following values
/// with your OpenTok API key as well as a test session ID and token.
// See https://dashboard.tokbox.com/. In a real application, you will use 
// an OpenTok server SDK to generate session IDs and tokens.
var apiKey = '';
var sessionId = '';
var token = '';

// Set this to true and signals initiated while reconnecting to a session
// will be sent when reconnected:
var retrySignalOnReconnect = false;
